import React from "react";

export default function Header() {
  return (
    <header
      style={{
        height: 60,
        background: "#1f2937",
        color: "#fff",
        display: "flex",
        alignItems: "center",
        padding: "0 20px",
        fontWeight: "bold",
      }}
    >
      D'GUST ERP
    </header>
  );
}
